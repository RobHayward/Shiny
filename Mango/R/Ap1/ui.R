require(shiny)
fluidPage(
  
  titlePanel(), # Add title
  
  sidebarLayout(
    
    sidebarPanel(), # add sidebar
    
    
    mainPanel() # add main panel
    
  ) # Close side layout
  
  
) # Close Page